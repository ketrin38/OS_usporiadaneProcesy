CC=gcc
CFLAGS=-std=c99 -Wall -Wextra -O2 -s -pedantic -pipe -I.
OBJ=Front.o Main.o Parser.o Matica.o
LIBS=-lpthread
APPNAME=usp_procesy
RES_DIR=../bin

all: $(OBJ)
	@-gcc -o $(RES_DIR)/$(APPNAME) $^ $(CFLAGS) $(LIBS)

%.o: %.c %.h
	@-$(CC) -c $(CFLAGS) -o $@ $<

clean:
	@-rm -rf *.o
